<!DOCTYPE HTML>
<html>
<head>
<title>My QR Generator</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Manrope&display=swap" rel="stylesheet">
<style>
    *{
        font-family: 'Manrope', sans-serif;
    }

    body{
        background-color:#f3f3f3;
    }
</style>
</head>
<body>
    <!-- As a link -->
<nav class="navbar navbar-light bg-white shadow">
    <div class="container-fluid">
      <a class="navbar-brand fs-4 fw-bold" href="#">QR Code Generator</a>
    </div>
</nav>

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card card-default shadow">
                <div class="card-header bg-danger text-white fw-bold fs-5">QR Code</div>
                <div class="card-body">
                <% if(QR_code){ %>
                <h3 class="text-center">Scan this from your scanner</h3>
                <div class="text-center my-3">
                <img src="<%= QR_code %>">
                <br>
                <a class="btn btn-sm btn-danger" href="/download?file_path=<%= img_src %>"> Download</a>
                </div>
                <% }else{ %>
                    <form action="/" class="p-5" method="POST">
                        <div class="form-group p-1">
                            <!-- <label for="url">Enter URL</label> -->
                            <input type="text" name="url" id="url" class="form-control rounded-2" required placeholder="Enter URL here..">
                        </div>
                        <div class="form-group p-1 text-center">
                            <button type="submit" class="btn btn-sm btn-danger">Generate</button>
                        </div>
                    </form>
                <% } %>
                </div>
            </div>
        </div>
    </div>
</div>


</body>
</html>